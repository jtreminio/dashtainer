server {
    listen *:8080 default_server;

    server_name _;
    root {{ form.document_root }};

    index index.html;
    autoindex off;

    location / {
        try_files $uri $uri/ =404;
    }
}
