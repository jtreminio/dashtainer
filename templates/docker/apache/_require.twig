{% for require in requires|default({}) %}
    {% if require is hash %}
    <Require{{ require.enforce | capitalize }}>
        {% for rule in require.rules|default([]) %}
        Require {{ rule | trim }}
        {% endfor %}
    </Require{{ require.enforce | capitalize }}>
    {% else %}
    Require {{ require | trim }}
    {% endif %}
{% endfor %}
