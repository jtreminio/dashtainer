#!/bin/bash
set -e
set -o pipefail

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

cat > "${DIR}/../docker/.env" << EOF
COMPOSE_PROJECT_NAME=dash
USER_ID=$(id -u ${USER})
GROUP_ID=$(id -g ${USER})
EOF

exit 0
